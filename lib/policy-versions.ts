// Bump this when real Terms/Privacy/Guidelines content replaces the current
// placeholder text — every new sign-up will then record acceptance of the
// new version. This does NOT retroactively re-prompt existing users to
// re-accept; that's a separate, not-yet-built feature.
export const CURRENT_POLICY_VERSION = '2026-09-14-placeholder'
